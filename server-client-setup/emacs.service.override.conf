# Make the Emacs daemon respawn no matter how it dies (crash, OOM, or an
# in-Emacs `kill-emacs'). An explicit `systemctl --user stop emacs' still
# stops it — Restart=always does not fight a deliberate stop.
[Service]
Restart=always
RestartSec=2
